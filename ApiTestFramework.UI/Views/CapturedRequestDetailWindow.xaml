<Window x:Class="ApiTestFramework.UI.Views.CapturedRequestDetailWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:converters="clr-namespace:ApiTestFramework.UI.Converters"
        Title="请求详情" Height="560" Width="760"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False">

    <Window.Resources>
        <converters:StatusCodeToColorConverter x:Key="StatusCodeColorConverter"/>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 顶部：方法 + 状态码 + URL -->
        <Border Grid.Row="0" Background="#FAFAFA" Padding="10" BorderBrush="#DDD" BorderThickness="0,0,0,1">
            <StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="{Binding Method}" FontWeight="Bold" FontSize="14" Margin="0,0,15,0"
                               VerticalAlignment="Center"/>
                    <TextBlock Text="状态码: " Foreground="#666" VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding StatusCodeText}" FontWeight="Bold" VerticalAlignment="Center"
                               Foreground="{Binding StatusCode, Converter={StaticResource StatusCodeColorConverter}}"
                               Margin="0,0,15,0"/>
                    <TextBlock Text="序号: " Foreground="#666" VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding Order}" FontWeight="Bold" VerticalAlignment="Center"/>
                </StackPanel>
                <TextBlock Text="{Binding Url}" TextWrapping="Wrap" Foreground="#555" Margin="0,4,0,0"/>
            </StackPanel>
        </Border>

        <!-- 请求头与请求体 -->
        <TabControl Grid.Row="1" Padding="0">
            <TabItem Header="Headers">
                <DataGrid ItemsSource="{Binding Headers}"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          IsReadOnly="True"
                          HeadersVisibility="Column"
                          GridLinesVisibility="Horizontal"
                          BorderThickness="0">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Key" Binding="{Binding Key}" Width="*"/>
                        <DataGridTextColumn Header="Value" Binding="{Binding Value}" Width="2*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>

            <TabItem Header="Body">
                <TextBox Text="{Binding Body}"
                         IsReadOnly="True"
                         AcceptsReturn="True"
                         VerticalScrollBarVisibility="Auto"
                         HorizontalScrollBarVisibility="Auto"
                         FontFamily="Consolas"
                         FontSize="12"
                         Padding="10"
                         BorderThickness="0"/>
            </TabItem>
        </TabControl>
    </Grid>
</Window>
