<UserControl x:Class="ApiTestFramework.Components.SeedDataDetailControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:ApiTestFramework.Components"
             xmlns:converters="clr-namespace:ApiTestFramework.Converters"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#FAFAFA" Padding="10" BorderBrush="#DDD" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal">
                <Button Content="上传文件"
                        Command="{Binding UploadFileCommand}"
                        Background="#2196F3"
                        Foreground="White"
                        BorderThickness="0"
                        Padding="15,8"
                        Margin="0,0,5,0"
                        Cursor="Hand"/>
                <Button Content="保存"
                        Command="{Binding SaveFileCommand}"
                        Background="#FF9800"
                        Foreground="White"
                        BorderThickness="0"
                        Padding="15,8"
                        Margin="0,0,5,0"
                        Cursor="Hand"/>
                <Button Content="执行"
                        Command="{Binding ExecuteCommand}"
                        Background="#4CAF50"
                        Foreground="White"
                        BorderThickness="0"
                        Padding="15,8"
                        Margin="0,0,5,0"
                        Cursor="Hand"/>
                <Button Content="删除"
                        Command="{Binding DeleteFileCommand}"
                        Background="#F44336"
                        Foreground="White"
                        BorderThickness="0"
                        Padding="15,8"
                        Cursor="Hand"/>
                <ProgressBar Width="20"
                             Height="20"
                             Margin="10,0,0,0"
                             IsIndeterminate="{Binding IsExecuting}"
                             Visibility="{Binding IsExecuting, Converter={StaticResource BooleanToVisibilityConverter}}"/>
            </StackPanel>
        </Border>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="#F5F5F5" BorderBrush="#DDD" BorderThickness="0,0,1,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Background="#E8E8E8" Padding="10,8">
                        <TextBlock Text="种子文件列表" FontWeight="Bold" FontSize="12"/>
                    </Border>

                    <ListBox Grid.Row="1"
                             ItemsSource="{Binding SeedFiles}"
                             SelectedItem="{Binding SelectedFile}"
                             BorderThickness="0"
                             Background="Transparent">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" Margin="5,3">
                                    <TextBlock Text="📄" Margin="0,0,5,0" FontSize="12"/>
                                    <TextBlock Text="{Binding}" VerticalAlignment="Center"/>
                                </StackPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </Border>

            <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Center" VerticalAlignment="Stretch" Background="#DDD"/>

            <TextBox Grid.Column="2"
                     Text="{Binding FileContent, UpdateSourceTrigger=PropertyChanged}"
                     AcceptsReturn="True"
                     AcceptsTab="True"
                     VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Auto"
                     FontFamily="Consolas"
                     FontSize="12"
                     Padding="10"
                     BorderThickness="0"/>
        </Grid>

        <Border Grid.Row="2" Background="#E8E8E8" Padding="10,5" BorderBrush="#DDD" BorderThickness="0,1,0,0">
            <TextBlock Text="{Binding ResultMessage}" Foreground="#666" TextWrapping="Wrap"/>
        </Border>
    </Grid>
</UserControl>
